<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>W4ke</title>
  
  <subtitle>Welcome to my blog!</subtitle>
  <link href="http://www.w4ke.cn/atom.xml" rel="self"/>
  
  <link href="http://www.w4ke.cn/"/>
  <updated>2020-11-20T16:32:46.409Z</updated>
  <id>http://www.w4ke.cn/</id>
  
  <author>
    <name>W4ke</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Docker简单使用教程</title>
    <link href="http://www.w4ke.cn/2020/11/20/Docker%E7%AE%80%E5%8D%95%E4%BD%BF%E7%94%A8%E6%95%99%E7%A8%8B/"/>
    <id>http://www.w4ke.cn/2020/11/20/Docker%E7%AE%80%E5%8D%95%E4%BD%BF%E7%94%A8%E6%95%99%E7%A8%8B/</id>
    <published>2020-11-20T12:35:00.000Z</published>
    <updated>2020-11-20T16:32:46.409Z</updated>
    
    <content type="html"><![CDATA[<p>Docker 是一个开源的应用容器引擎，基于 Go 语言 并遵从 Apache2.0 协议开源。</p><p>Docker 可以让开发者打包他们的应用以及依赖包到一个轻量级、可移植的容器中，然后发布到任何流行的 Linux 机器上，也可以实现虚拟化。</p><p>容器是完全使用沙箱机制，相互之间不会有任何接口（类似 iPhone 的 app）,更重要的是容器性能开销极低。</p><p>说白了就是一个开销更低，安装更便捷的虚拟机。</p><p>这里介绍一点简单的Docker使用教程，详细教程参考<a href="https://www.runoob.com/docker/docker-tutorial.html">菜鸟教程</a></p><hr><h1 id="Docker-基本语法"><a href="#Docker-基本语法" class="headerlink" title="Docker 基本语法"></a>Docker 基本语法</h1><h2 id="安装-Docker"><a href="#安装-Docker" class="headerlink" title="安装 Docker"></a>安装 Docker</h2><p><code>Ubuntu</code>下使用命令:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">apt-get install docker.io<br>curl -sSL https:&#x2F;&#x2F;get.daocloud.io&#x2F;docker | sh<br></code></pre></td></tr></table></figure><h2 id="下载镜像"><a href="#下载镜像" class="headerlink" title="下载镜像"></a>下载镜像</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker pull + 镜像名:标签<br>eg:docker pull ubuntu:20.04<br></code></pre></td></tr></table></figure><p>Docker 官方提供了Docker Hub仓库，所有用户都可以向Docker Hub贡献自己制作的Docker镜像。</p><h2 id="创建容器"><a href="#创建容器" class="headerlink" title="创建容器"></a>创建容器</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker run -it -p 90:80 ubuntu:20.04   &#x2F;bin&#x2F;bash<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/2020092818252673.png#pic_center" alt="在这里插入图片描述"><br><code>-i</code>  在新容器内指定一个伪终端或终端。<br><code>-t</code>  允许你对容器内的标准输入 (STDIN) 进行交互。<br><code>-p</code>  进行端口映射。<code>90:80</code>指的是将主机的<code>90</code>端口映射到容器的<code>80</code>端口。<br><code>ubuntu:20.04</code> 镜像名:标签，这里调用20.04版本的ubuntu系统，如果本地上没有相关镜像会自动从Docker Hub上下载。<br><code>/bin/bash</code>：放在镜像名后的是命令，这里我们希望有个交互式 Shell，因此用的是 /bin/bash。<br>运行完成以后，直接进入了Docker容器，这里基本上可以把它当成一个真实的ubuntu机进行使用。输入<code>exit</code>退出容器，退出时容器关闭。<br><img src="https://img-blog.csdnimg.cn/20200928190523513.png#pic_center" alt="在这里插入图片描述"></p><p>如果希望建立一个后台运行的Docker容器的话，可以采用。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker run -dt -p 90:80 ubuntu:20.04   &#x2F;bin&#x2F;bash<br></code></pre></td></tr></table></figure><p><code>-d</code>指的是进行后台运行。</p><h2 id="开启-关闭-容器"><a href="#开启-关闭-容器" class="headerlink" title="开启/关闭 容器"></a>开启/关闭 容器</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker ps -a    #列出所有容器<br>docker ps    #列出正在运行的容器<br>docker start + 容器id    #开启容器<br>docker stop  +容器id    #关闭容器<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928191130433.png?x-oss-process=image" alt="在这里插入图片描述"><br>输出详情介绍：<br><code>CONTAINER ID</code>: 容器 ID。<br><code>IMAGE</code>: 使用的镜像。<br><code>COMMAND</code>: 启动容器时运行的命令。<br><code>CREATED</code>: 容器的创建时间。<br><code>STATUS</code>: 容器状态。<br><code>PORTS</code>: 容器的端口信息和使用的连接类型（tcp\udp）。<br><code>NAMES</code>: 自动分配的容器名称。</p><h2 id="主机连接Docker"><a href="#主机连接Docker" class="headerlink" title="主机连接Docker"></a>主机连接Docker</h2><p>使用<code>exit</code>退出后，Docker默认会关闭掉，使用<code>docker start</code>重启容器但是并没有进入容器，此时可以用<code>docker attach</code>或者<code>docker exec</code>命令来进行连接。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker attach + 容器id<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/2020092819264111.png#pic_center" alt="在这里插入图片描述"><br>这个命令有个缺点，使用<code>exit</code>退出后容器不会在后台运行，而是跟原来一样被关闭，而<code>docker exec</code>不会有这个问题。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker exec -it +容器id  &#x2F;bin&#x2F;bash<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/2020092819291651.png#pic_center" alt="在这里插入图片描述"></p><h2 id="导出-导入容器"><a href="#导出-导入容器" class="headerlink" title="导出/导入容器"></a>导出/导入容器</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker export 容器id  &gt; ubuntu.tar<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928193152138.png#pic_center" alt="在这里插入图片描述"><br>这里将整个容器导出成<code>ubuntu.tar</code>这个包，就可以进行转移。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">cat ubuntu.tar | docker import - test&#x2F;ubuntu:v1<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928193528990.png#pic_center" alt="在这里插入图片描述"><br>这里将<code>ubuntu.tar</code>导入成镜像，镜像名叫<code>test/ubuntu</code>，标签为<code>v1</code></p><h2 id="删除容器"><a href="#删除容器" class="headerlink" title="删除容器"></a>删除容器</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker rm +容器id<br></code></pre></td></tr></table></figure><p>正在运行的容器必须先停下再删除<br><img src="https://img-blog.csdnimg.cn/20200928195519611.png?x-oss-process=image" alt="在这里插入图片描述"></p><h2 id="Docker镜像"><a href="#Docker镜像" class="headerlink" title="Docker镜像"></a>Docker镜像</h2><ul><li><p>列出本地主机上的镜像。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker images <br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928195651609.png?x-oss-process=image" alt="在这里插入图片描述"><br><code>REPOSITORY</code>：表示镜像的仓库源<br><code>TAG</code>：镜像的标签<br><code>IMAGE ID</code>：镜像ID<br><code>CREATED</code>：镜像创建时间<br><code>SIZE</code>：镜像大小</p></li><li><p>查找镜像</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker search httpd<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/2020092820004138.png?x-oss-process=image" alt="在这里插入图片描述"></p></li><li><p>获取镜像</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker pull centos&#x2F;httpd-24-centos7<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928200156246.png?x-oss-process=image" alt="在这里插入图片描述"></p></li><li><p>删除镜像</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker rmi +镜像名字:标签<br>或者  docker rmi  镜像id<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928200449676.png?x-oss-process=image" alt="在这里插入图片描述"></p></li></ul><h2 id="创建镜像"><a href="#创建镜像" class="headerlink" title="创建镜像"></a>创建镜像</h2><ul><li>1.从已经创建的容器中更新镜像，并且提交这个镜像。<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker commit -m&#x3D;&quot;has update&quot; -a&#x3D;&quot;runoob&quot; e218edb10161 runoob&#x2F;ubuntu:v2<br></code></pre></td></tr></table></figure>各个参数说明：<br><code>-m</code>: 提交的描述信息<br><code>-a</code>: 指定镜像作者<br><code>e218edb10161</code>：容器 ID<br><code>1275178869/ubuntu:v2</code>: 指定要创建的目标镜像名</li></ul><ul><li>2.使用 Dockerfile 指令来创建一个新的镜像。</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs plain">#Dockerfile 内容<br>FROM    centos:6.7<br>MAINTAINER      Fisher &quot;fisher@sudops.com&quot;<br><br>RUN     &#x2F;bin&#x2F;echo &#39;root:123456&#39; |chpasswd<br>RUN     useradd runoob<br>RUN     &#x2F;bin&#x2F;echo &#39;runoob:123456&#39; |chpasswd<br>RUN     &#x2F;bin&#x2F;echo -e &quot;LANG&#x3D;\&quot;en_US.UTF-8\&quot;&quot; &gt;&#x2F;etc&#x2F;default&#x2F;local<br>EXPOSE  22<br>EXPOSE  80<br>CMD     &#x2F;usr&#x2F;sbin&#x2F;sshd -D<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928201119783.png?x-oss-process=image" alt="在这里插入图片描述"><br>每一个指令都会在镜像上创建一个新的层，每一个指令的前缀都必须是大写的。<br>第一条FROM，指定使用哪个镜像源<br>RUN 指令告诉docker 在镜像内执行命令，安装了什么。。。<br>然后，我们使用 Dockerfile 文件，通过 docker build 命令来构建一个镜像。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker build -t runoob&#x2F;centos:6.7 .<br></code></pre></td></tr></table></figure><p>不要忘了该命令的最后一个<code>.</code>，这里指的是Dockerfile所在的路径，<code>.</code>表示Dockerfile就在当前路径下。<br><img src="https://img-blog.csdnimg.cn/20200928201538242.png?x-oss-process=image" alt="在这里插入图片描述"><br>查看一下镜像：<br><img src="https://img-blog.csdnimg.cn/20200928201739324.png?x-oss-process=image" alt="在这里插入图片描述"><br>多了一个镜像，就是刚才创建的镜像。<br>具体Dockerfile的编写后面会有介绍，这里就不赘叙了。</p><h2 id="设置镜像标签"><a href="#设置镜像标签" class="headerlink" title="设置镜像标签"></a>设置镜像标签</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker tag 镜像id 镜像名字:标签<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/20200928202043410.png?x-oss-process=image" alt="在这里插入图片描述"></p><hr><h1 id="Docker之Dockerfile"><a href="#Docker之Dockerfile" class="headerlink" title="Docker之Dockerfile"></a>Docker之Dockerfile</h1><p><a href="https://www.runoob.com/docker/docker-dockerfile.html">Dockerfile菜鸟教程</a></p><p>说白了，Dockerfile就是告诉Docker调用哪个镜像，执行哪些命令，执行完之后将容器导出为镜像。</p><p>Dockerfile的编写主要是各种指令的调用，堆在一起就好了。</p><h2 id="FROM-和-RUN-指令"><a href="#FROM-和-RUN-指令" class="headerlink" title="FROM 和 RUN 指令"></a>FROM 和 RUN 指令</h2><p><code>FROM</code>：定制的镜像都是基于 FROM 的镜像，这里的 nginx 就是定制需要的基础镜像。后续的操作都是基于 nginx。</p><p><code>RUN</code>：用于执行后面跟着的命令行命令。<br>举个栗子</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">FROM nginx       #调用nginx镜像<br>RUN echo &#39;这是一个本地构建的nginx镜像&#39; &gt; &#x2F;usr&#x2F;share&#x2F;nginx&#x2F;html&#x2F;index.html     #在&#x2F;usr&#x2F;share&#x2F;nginx&#x2F;html&#x2F;目录下建一个index.html内容为  &#39;这是一个本地构建的nginx镜像&#39;<br></code></pre></td></tr></table></figure><p><img src="https://img-blog.csdnimg.cn/202009282202546.png#pic_center" alt="在这里插入图片描述"><br>文件名必须是<code>Dockerfile</code>一点都不能错，内容就是这两行内容。</p><p>然后执行</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker build -t 镜像名称:标签 .<br></code></pre></td></tr></table></figure><p>注意千万别忘了最后那个点！<br><img src="https://img-blog.csdnimg.cn/20200928224009122.png?x-oss-process=image" alt="在这里插入图片描述"><br>查看一下镜像，发现多了一个镜像叫nginx<br><img src="https://img-blog.csdnimg.cn/20200928224131382.png#pic_center" alt="在这里插入图片描述"><br>尝试着运行一下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">docker run -it -p 900:80 nginx:v1 &#x2F;bin&#x2F;bash<br></code></pre></td></tr></table></figure><p>在这里将主机的900端口映射到容器的80端口，因为访问网页服务器都是默认在80端口。<br>然后访问一波：<br><img src="https://img-blog.csdnimg.cn/20200928231050319.png?x-oss-process=image" alt="在这里插入图片描述"><br>Successful!</p><h2 id="COPY指令"><a href="#COPY指令" class="headerlink" title="COPY指令"></a>COPY指令</h2><p>复制指令，从上下文目录中复制文件或者目录到容器里指定路径。</p><p>这个命令最强大的作用是可以将主机的文件拷贝到容器中，不然需要建共享文件夹等很麻烦的操作。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">COPY .&#x2F;file  &#x2F;tmp<br></code></pre></td></tr></table></figure><p>将<code>file</code>目录以及其下的子目录全部拷贝到容器的<code>/tmp</code>文件夹中，接下去对文件的操作写在RUN指令中即可。</p><h2 id="CMD"><a href="#CMD" class="headerlink" title="CMD"></a>CMD</h2><p>类似于 RUN 指令，用于运行程序，但二者运行的时间点不同：</p><ul><li><code>CMD</code> 在docker run 时运行。</li><li><code>RUN</code>是在 docker build。</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plain">CMD &lt;shell 命令&gt; <br>CMD [&quot;&lt;可执行文件或命令&gt;&quot;,&quot;&lt;param1&gt;&quot;,&quot;&lt;param2&gt;&quot;,...] <br>CMD [&quot;&lt;param1&gt;&quot;,&quot;&lt;param2&gt;&quot;,...]  # 该写法是为 ENTRYPOINT 指令指定的程序提供默认参数<br></code></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">CMD[&quot;service&quot;,&quot;apache2&quot;,&quot;start&quot;]<br></code></pre></td></tr></table></figure><p>在run容器的时候开启<code>Apache2</code>服务。</p><p><font color="red"><strong>注意：如果 Dockerfile 中如果存在多个 CMD 指令，仅最后一个生效。</strong></font></p><p>所以如果需要执行的命令比较多的话可以写一个sh脚本，直接在CMD里运行这个脚本即可。</p><h2 id="EXPOSE"><a href="#EXPOSE" class="headerlink" title="EXPOSE"></a>EXPOSE</h2><p>仅仅只是声明端口。<br>作用：</p><ul><li><p>帮助镜像使用者理解这个镜像服务的守护端口，以方便配置映射。</p></li><li><p>在运行时使用随机端口映射时，也就是 docker run -P 时，会自动随机映射 EXPOSE 的端口。</p></li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">EXPOSE &lt;端口1&gt; [&lt;端口2&gt;...]<br></code></pre></td></tr></table></figure><h2 id="WORKDIR"><a href="#WORKDIR" class="headerlink" title="WORKDIR"></a>WORKDIR</h2><p>指定工作目录。用 WORKDIR 指定的工作目录，会在构建镜像的每一层中都存在。（WORKDIR 指定的工作目录，必须是提前创建好的）。</p><p>docker build 构建镜像过程中的，每一个 RUN 命令都是新建的一层。只有通过 WORKDIR 创建的目录才会一直存在。</p><h2 id="一个具体的示例"><a href="#一个具体的示例" class="headerlink" title="一个具体的示例"></a>一个具体的示例</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs plain">FROM ctftraining&#x2F;base_image_nginx_mysql_php_56        #导入镜像<br><br>COPY src &#x2F;var&#x2F;www&#x2F;html                                #将文件复制到&#x2F;var&#x2F;www&#x2F;html<br><br>RUN chown -R www-data:www-data &#x2F;var&#x2F;www&#x2F;html&#x2F;uploads &amp;&amp; \                #执行这三条指令<br>    mv &#x2F;var&#x2F;www&#x2F;html&#x2F;flag.sh &#x2F; &amp;&amp; \<br>    chmod +x &#x2F;flag.sh<br><br>EXPOSE 80                            #声明80端口<br><br>CMD[&quot;service&quot;,&quot;apache2&quot;,&quot;start&quot;]                 #开启容器时<br></code></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Docker 是一个开源的应用容器引擎，基于 Go 语言 并遵从 Apache2.0 协议开源。&lt;/p&gt;
&lt;p&gt;Docker 可以让开发者打包他们的应用以及依赖包到一个轻量级、可移植的容器中，然后发布到任何流行的 Linux 机器上，也可以实现虚拟化。&lt;/p&gt;
&lt;p&gt;容器是</summary>
      
    
    
    
    <category term="Docker" scheme="http://www.w4ke.cn/categories/Docker/"/>
    
    
    <category term="Docker" scheme="http://www.w4ke.cn/tags/Docker/"/>
    
  </entry>
  
</feed>
